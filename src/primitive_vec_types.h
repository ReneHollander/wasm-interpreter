#ifndef PRIMITIVE_VEC_TYPES_H
#define PRIMITIVE_VEC_TYPES_H

#include "value.h"
#include "vec.h"

CREATE_VEC(byte, byte)

CREATE_VEC(typeidx, typeidx)

CREATE_VEC(funcidx, funcidx)

CREATE_VEC(labelidx, labelidx)

#endif // PRIMITIVE_VEC_TYPES_H
